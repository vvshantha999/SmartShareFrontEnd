import {EmailToUsernamePipePipe} from './email-to-username-pipe.pipe';

describe('EmailToUsernamePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EmailToUsernamePipePipe();
    expect(pipe).toBeTruthy();
  });
});
